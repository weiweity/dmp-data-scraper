# DMP Data Scraper Dependencies
# Last updated: 2026-06-13
#
# ⚠️ 此文件仅为 `pip install -r requirements.txt` 兼容性保留
# 真实依赖声明在 pyproject.toml (推荐用 `pip install -e .` 或 uv)

# Browser automation
playwright>=1.40.0

# YAML config
pyyaml>=6.0

# Numerical computing (anti_detect.py 用 np.random.normal)
numpy>=1.24.0
